# 清理记录（2026-03-14）

## 本次清理目标
- 保留主训练产物与主数据集。
- 删除明显临时/冒烟/测试产物，降低目录混乱度。

## 已执行清理
1. 删除 `runs/mppi*` 评测/冒烟目录。
2. 删除 `datasets/` 中 `test_*` 与 `*smoke*` 文件。
3. 删除全项目 `__pycache__/`。
4. 删除 `MUJOCO_LOG.TXT`。

## 保留内容
- `runs/icode_e1_mix_stageA`
- `runs/icode_e1_mix_stageB`
- `runs/icode_e1_opt_30min_rollout`
- `runs/icode_e1_passable35_uniform_4h`
- `datasets/` 下主数据集与训练 bundle

## 备注
- 本仓库 `.gitignore` 已忽略 `runs/` 与 `datasets/`，因此清理动作主要影响本地磁盘，不直接体现在 git 追踪文件删除上。
